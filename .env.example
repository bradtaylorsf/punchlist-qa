# Required: GitHub PAT with repo scope
PUNCHLIST_GITHUB_TOKEN=

# Required: JWT auth secret (generate: openssl rand -hex 32)
PUNCHLIST_AUTH_SECRET=

# Optional
# PORT=4747
# HOST=127.0.0.1
# PUNCHLIST_DATA_DIR=.punchlist/
# NODE_ENV=production
